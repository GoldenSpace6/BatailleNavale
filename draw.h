#ifndef FUNCTION
 #define FUNCTION
 #include "functions.h"
#endif
//Print the character to show for each State
void printSlotState(enum state a,int k);
//Draw a Board
//Board,Bool to draw boat or not
void drawBoard(Board p,int k);
//Draw both Boards side by side
void drawBoards(Game g);
